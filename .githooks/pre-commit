#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
    echo "BLOCKED: direct commits to main are disabled."
    echo "Create a feature branch first."
    exit 1
fi
